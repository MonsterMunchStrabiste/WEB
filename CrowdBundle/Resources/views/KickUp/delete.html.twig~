{% extends "WEBCrowdBundle::layout.html.twig" %}

{% block title %}
  Supprimer une annonce - {{ parent() }}
{% endblock %}
{% block body %}
<div class="jumbotron under-navbar">
  <h2>Supprimer une annonce</h2>
  <p>
    Etes-vous certain de vouloir supprimer l'annonce "{{ advert.title }}" ?
  </p>
</div>
  <form action="{{ path('web_crowd_delete', {'id': advert.id}) }}" method="post">
    <a href="{{ path('web_crowd_view', {'id': advert.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à l'annonce
    </a>
    <input type="submit" value="Supprimer" class="btn btn-danger" />
    {# Ceci va générer le champ CSRF #}
    {{ form_rest(form) }}
  </form>

{% endblock %}
